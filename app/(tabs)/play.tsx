// This file satisfies the Expo Router route for the "play" tab.
// Navigation is handled by the custom PlayTabButton in _layout.tsx.
import { View } from 'react-native';
export default function PlayScreen() {
  return <View />;
}
